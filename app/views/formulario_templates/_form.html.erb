<%= form_with(model: formulario_template) do |form| %>
  <% if formulario_template.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(formulario_template.errors.count, "error") %> prohibited this formulario_template from being saved:</h2>

      <ul>
        <% formulario_template.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :forms_id, style: "display: block" %>
    <%= form.number_field :forms_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
