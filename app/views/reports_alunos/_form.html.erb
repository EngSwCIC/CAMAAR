<%= form_with(model: reports_aluno) do |form| %>
  <% if reports_aluno.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(reports_aluno.errors.count, "error") %> prohibited this reports_aluno from being saved:</h2>

      <ul>
        <% reports_aluno.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :formulario_id, style: "display: block" %>
    <%= form.number_field :formulario_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
