<!DOCTYPE html>
<html>
<head>
  <title>Detalhes do Template</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f0f0f0;
      margin: 0;
      padding: 0;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
    }
    main {
      background-color: #fff;
      padding: 20px;
      border-radius: 8px;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
      width: 500px;
    }
    h1 {
      color: #333;
      margin-bottom: 20px;
    }
    #metadata {
      margin-bottom: 20px;
    }
    #details {
      list-style: none;
      padding: 0;
    }
    #details li {
      margin: 10px 0;
      padding: 10px;
      background-color: #f9f9f9;
      border: 1px solid #ddd;
      border-radius: 4px;
    }
    .btn {
      display: inline-block;
      padding: 10px 20px;
      margin-top: 10px;
      background-color: #007BFF;
      color: #fff;
      text-decoration: none;
      border-radius: 4px;
      margin-right: 10px;
      text-align: center;
    }
    .btn:hover {
      background-color: #0056b3;
    }
    .btn-primary {
      background-color: #28a745;
    }
    .btn-primary:hover {
      background-color: #218838;
    }
  </style>
</head>
<body>
  <main>
    <h1>Details about <%= @template.nome %></h1>

    <div id="metadata">
      <ul id="details">
        <li>ID: <%= @template.id %></li>
        <li>Nome: <%= @template.nome %></li>
        <li>Criado em: <%= @template.created_at.strftime('%d/%m/%Y %H:%M') %></li>
        <li>Última atualização: <%= @template.updated_at.strftime('%d/%m/%Y %H:%M') %></li>
      </ul>
    </div>

    <%= link_to 'Editar o nome deste template', edit_template_path(@template), class: 'btn' %>
    <%= link_to 'Voltar para templates', templates_path, class: 'btn btn-primary' %>
  </main>
</body>
</html>