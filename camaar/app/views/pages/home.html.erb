<%= stylesheet_link_tag "base" %>
<%= stylesheet_link_tag "assessments" %>
<link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400&display=swap" rel="stylesheet">

<body>
<div class="header-bar">
  <div class="left-frame">
    <button class="menu-button">&#9776;</button>
    <span class="header-text">Avaliações</span>
  </div>
  <div class="search-bar">
    <input type="text">
    <button class="search-icon">&#128269;</button>
  </div>
  <button class="user-button">U</button>
</div>
<div class="content-frame">
  <div class="side-bar">
    <div class="select-button">Avaliações</div>
    <div class="select-button">Gerenciamento</div>
  </div>

  <div class="assessments-container">
    <% current_user.turmas.each do |turma| %>
      <% turma.formulario.each do |formulario| %>
        <% if Date.today < formulario.dataDeTermino %>
          <%= link_to formulario_path(formulario) do %>
            <div class="assessments-box">
              <div class="subject-name"><%= turma.nome_materia %></div>
              <div class="semester"><%= turma.semestre %></div>
              <div class="professor"><%= turma.nome_professor %></div>
            </div>
          <% end %>
        <% end %>
      <% end %>
    <% end %>
  </div>
    
</div>

</body>